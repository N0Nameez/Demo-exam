<UserControl x:Class="retail.View.OrderCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:retail.View"
             mc:Ignorable="d"
             MinHeight="100"
            Height="Auto" Width="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="{Binding Article, StringFormat=Артикул заказа: {0}}"
                   Margin="5"/>

            <TextBlock Text="{Binding Status, StringFormat=Статус заказа: {0}}"
                   Margin="5"/>

            <TextBlock Text="{Binding PickupPoint.Address, StringFormat=Адрес: {0}}"
                   Margin="5"/>

            <TextBlock Text="{Binding OrederDate, StringFormat=Дата заказа: {0:dd.MM.yyyy}}"
                   Margin="5"/>
        </StackPanel>

        <TextBlock Grid.Column="1"
                   Margin="5"
               HorizontalAlignment="Right" VerticalAlignment="Center"
               Text="{Binding DeliveryDate, StringFormat=Дата доставки: {0:dd.MM.yyyy}}"/>
    </Grid>
</UserControl>
